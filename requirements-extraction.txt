# GPU street-imagery extraction (Windows/Linux PC). Installed by run_extraction.bat.
# Torch CUDA wheels come from the PyTorch index; versions match what the
# pipeline was smoke-tested against (RTX 3070 = Ampere, supported by cu126).
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.0+cu126; sys_platform == 'win32'
torchvision==0.27.1+cu126; sys_platform == 'win32'
torch>=2.12,<2.13; sys_platform != 'win32'
torchvision>=0.27,<0.28; sys_platform != 'win32'
transformers==5.12.1
pillow>=10.0
pandas>=2.0
pyarrow>=14.0
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
requests>=2.31
mapbox-vector-tile==2.2.0
scipy>=1.11
tqdm>=4.66
